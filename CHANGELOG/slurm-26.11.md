## Changes in 26.11.0

* squeue/scontrol - Add REQUEUE_PREEMPT (RP) and REQUEUE_NODE_FAIL (RN) job state flags so a job requeued because it was preempted can be distinguished from one requeued because of a node failure while it is pending again.
