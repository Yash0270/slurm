# cgroup.conf - kept minimal for a container-in-container test cluster.
# Constraints are disabled because nested cgroup control is unreliable in
# Docker; proctrack/linuxproc + task/none are used instead (see slurm.conf).
CgroupPlugin=disabled
ConstrainCores=no
ConstrainRAMSpace=no
ConstrainDevices=no
